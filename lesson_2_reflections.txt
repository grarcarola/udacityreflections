What happens when you initialize a repository? Why do you need to do it?

It creates hidden .git folders and allows you to commit files in the folder.
You need to do it so git will run. Otherwise commands wil fail.

How is the staging area different from the working directory and the repository? What value do you think it offers?

It's sort of a inbetween stage. The working directory inculdes all files but the repository and staging are might not. It must be passed through.